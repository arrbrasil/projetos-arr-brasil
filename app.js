const $=x=>document.getElementById(x),fmt=n=>new Intl.NumberFormat('pt-BR').format(n),compact=n=>new Intl.NumberFormat('pt-BR',{notation:'compact',maximumFractionDigits:1}).format(n);let page=1,filtered=[];const fields=['registry','status','category','method'];
function esc(s){return String(s||'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]))}
function options(id,key){$(id).innerHTML='<option>Todos</option>'+[...new Set(PROJECTS.map(x=>x[key]).filter(Boolean))].sort().map(x=>'<option>'+esc(x)+'</option>').join('')}
function count(key,n=8){const o={};filtered.forEach(x=>o[x[key]||'Não informado']=(o[x[key]||'Não informado']||0)+1);return Object.entries(o).sort((a,b)=>b[1]-a[1]).slice(0,n)}
function bars(id,data,color){const max=Math.max(...data.map(x=>x[1]),1);$(id).innerHTML=data.map(x=>'<div class="bar"><div class="barMeta"><span>'+esc(x[0])+'</span><b>'+fmt(x[1])+'</b></div><div class="track"><i style="width:'+(x[1]/max*100)+'%;background:'+color+'"></i></div></div>').join('')}
function render(){const q=$('q').value.toLowerCase().trim();filtered=PROJECTS.filter(x=>fields.every(k=>$(k).value==='Todos'||x[k]===$(k).value)&&(!q||[x.id,x.name,x.developer,x.state].some(v=>(v||'').toLowerCase().includes(q))));const pages=Math.max(1,Math.ceil(filtered.length/15));page=Math.min(page,pages);$('total').textContent=fmt(filtered.length);const cert=filtered.filter(x=>x.status==='Registered'||x.status==='Gold Standard Certified Project').length;$('certified').textContent=fmt(cert);$('certifiedPct').textContent=filtered.length?Math.round(cert/filtered.length*100)+'% do recorte':'—';const v=filtered.filter(x=>x.registry==='Verra').length;$('split').textContent=v+' / '+(filtered.length-v);$('credits').textContent=compact(filtered.reduce((a,x)=>a+(Number(x.credits)||0),0))+' tCO₂e';$('results').textContent=fmt(filtered.length)+' resultados';bars('states',count('state'),'#1f7a59');bars('statuses',count('status',7),'#d29a45');$('body').innerHTML=filtered.slice((page-1)*15,page*15).map((x,i)=>'<tr data-i="'+((page-1)*15+i)+'"><td class="id">'+esc(x.id)+'<small>'+esc(x.registry)+'</small></td><td><strong>'+esc(x.name||'Nome não informado')+'</strong><small>'+esc(x.developer||'Desenvolvedor não informado')+'</small></td><td>'+esc(x.state||'Não informado')+'</td><td><span class="tag">'+esc(x.category)+'</span></td><td>'+esc(x.method||'—')+'</td><td><span class="status '+((x.status==='Registered'||x.status==='Gold Standard Certified Project')?'ok':'')+'">'+esc(x.status)+'</span></td></tr>').join('');$('page').textContent='Página '+page+' de '+pages;$('prev').disabled=page===1;$('next').disabled=page===pages;document.querySelectorAll('tbody tr').forEach(tr=>tr.onclick=()=>detail(filtered[+tr.dataset.i]))}
function detail(x){$('detailTag').textContent=x.registry+' · PROJETO '+x.id;$('detailTitle').textContent=x.name||'Nome não informado';$('detailGrid').className='detailGrid';$('detailGrid').innerHTML=[['Situação',x.status],['Estado',x.state],['Desenvolvedor',x.developer],['Atividade',x.category],['Metodologia',x.method],['Créditos anuais',x.credits]].map(x=>'<div><span>'+x[0]+'</span><p>'+esc(x[1]||'Não informado')+'</p></div>').join('');$('detailLink').href=x.url;$('detail').showModal()}
options('registry','registry');options('status','status');options('category','category');options('method','method');[...fields,'q'].forEach(id=>$(id).oninput=()=>{page=1;render()});$('clear').onclick=()=>{fields.forEach(x=>$(x).value='Todos');$('q').value='';page=1;render()};$('prev').onclick=()=>{page--;render()};$('next').onclick=()=>{page++;render()};document.querySelector('.x').onclick=()=>$('detail').close();render();
